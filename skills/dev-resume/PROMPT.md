You are executing /dev-resume.

> **DEPRECATED**: Use `/dev-status` instead.
> This command is a backward-compatibility alias for `/dev-status`.
> Behavior is identical to `/dev-status`.

Proceed using `/dev-status` semantics.

---

## Redirect

`/dev-resume` → `/dev-status`

See `skills/dev-status/PROMPT.md` for full execution instructions.
